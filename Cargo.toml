[workspace]
resolver = "2"

members = [
    "apps/daemon",
    "apps/cli",
    "crates/domain",
    "crates/capabilities",
    "crates/policy",
    "crates/memory",
    "crates/storage",
    "crates/audit",
    "crates/protocol",
]

[workspace.package]
edition = "2024"
rust-version = "1.97"

[workspace.dependencies]
rusqlite = { version = "0.40.2", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tokio = { version = "1.53.1", default-features = false, features = ["io-util", "macros", "net", "rt", "signal", "time"] }

[workspace.lints.rust]
unsafe_code = "forbid"
